import java.util.Scanner;
public class Pal{
  	public static void main(String args[]) {

String inputSting, reverseString = "";
Scanner in  = new Scanner(System.in);

System.out.println("enter word");
inputSting = in.nextLine();

int length = inputSting.length();

for(int i = 0 ; && lt;= length - 1; i++ ){
reverseString += inputSting.charAt(length-1-i);
}
if(inputSting.equals(reverseString)){
	System.out.println("palindrom");
}else{
Sysetm.out.println("not palindrom")
	}
    }
}

